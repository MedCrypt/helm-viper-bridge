# FastAPI and server dependencies
# Note: Using Pydantic v1 for Python 3.9 compatibility
fastapi>=0.100.0,<0.110.0
uvicorn>=0.20.0
pydantic>=1.10,<2.0

# HTTP client for API calls
requests>=2.31.0
httpx>=0.28.0

# OAuth/SSO authentication
authlib>=1.6.0
itsdangerous>=2.0.0

# Protocol Buffers for Helm API
protobuf==3.20.3
